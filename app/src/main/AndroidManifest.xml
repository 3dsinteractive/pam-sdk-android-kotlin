<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pams.ai.demo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PamDemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Pam Demo"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PamDemo">
        <activity android:name=".cartPage.CartPage"></activity>
        <!-- Configs -->
        <meta-data
            android:name="pam-server"
            android:value="https:stg.pams.ai" />
        <meta-data
            android:name="public-db-alias"
            android:value="3digits-public" />
        <meta-data
            android:name="login-db-alias"
            android:value="3digits-login" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/logo_3digits" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/primary" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/noti_default_channel_id" />
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" /> <!-- Services -->
        <service
            android:name="services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service> <!-- Activities -->
        <activity android:name=".LaunchScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginPage" />
        <activity android:name=".notificationsPage.NotificationPage" />
        <activity android:name=".ProductDetailPage" />
        <activity android:name=".RegisterPage" />
        <activity android:name=".productsPage.ProductPage" />
    </application>

</manifest>