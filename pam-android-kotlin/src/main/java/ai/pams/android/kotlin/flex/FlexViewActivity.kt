package ai.pams.android.kotlin.flex

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

open class FlexViewActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
//        val flex = intent.getStringExtra("flex")

        // val parser = FlexLangParser()
        // val flexElement = parser.parse(flex)
        // flexElement?.render(this)
        // flexElement?.child?.get(0)?.props["src"]
    }
}
